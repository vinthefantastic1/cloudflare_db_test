version: '3.8'

services:
  wbs-app:
    build: .
    container_name: wbs-flask-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      # Cloudflare D1 Database Configuration
      # Replace these values with your actual Cloudflare credentials
      # OR create a .env file and uncomment the env_file line below
      - CF_ACCOUNT_ID=${CF_ACCOUNT_ID}
      - CF_D1_DATABASE_ID=${CF_D1_DATABASE_ID}
      - CF_API_TOKEN=${CF_API_TOKEN}
    # env_file:
    #   - .env
    volumes:
      # Persist database files
      - ./data:/app/data
      # Optional: Mount Excel files for loading data
      - ./excel_files:/app/excel_files:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add a reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   container_name: wbs-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - wbs-app
  #   restart: unless-stopped

networks:
  default:
    name: wbs-network